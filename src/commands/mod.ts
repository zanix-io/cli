import newCommand from 'commands/new/main.ts'
import prepareCommand from 'commands/prepare/main.ts'
import buildCommand from 'commands/build/main.ts'
import generateCommand from 'commands/generate/main.ts'
import spaceCommand from 'commands/space/main.ts'

export { buildCommand, generateCommand, newCommand, prepareCommand, spaceCommand }
